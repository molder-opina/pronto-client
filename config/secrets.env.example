# ==================== AUTHENTICATION SECRETS ====================

# Flask secret key (32+ caracteres aleatorios)
# Generar: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here

# Super admin handoff token pepper (32+ caracteres aleatorios)
# CRÍTICO: Debe estar configurado en producción (la app falla sin esto)
# Generar: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
HANDOFF_PEPPER=your-random-pepper-here-32chars-minimum

# Password hashing salt (32+ caracteres aleatorios)
# CRÍTICO: Debe estar configurado para que funcionen los logins
# Generar: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
PASSWORD_HASH_SALT=your-password-hash-salt-here-32chars-minimum

# Customer data encryption key (32+ caracteres aleatorios)
# CRÍTICO: Debe estar configurado para encriptar datos de clientes
# Generar: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
CUSTOMER_DATA_KEY=your-customer-data-encryption-key-here-32chars-minimum

# ==================== CORS CONFIGURATION ====================

# Producción: lista de orígenes permitidos separados por coma (sin espacios)
# Ejemplo: CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
# Dev: se usa allowlist interna (localhost:6080, localhost:6081, etc.)
CORS_ALLOWED_ORIGINS=

# ==================== PROXY CONFIGURATION ====================

# Número de proxies confiables delante de la app
# 0 = sin proxy (desarrollo local)
# 1 = un proxy (nginx directo)
# 2 = dos proxies en cadena (nginx + cloudflare)
# IMPORTANTE: Solo activar si el proxy sobrescribe X-Forwarded-* headers
NUM_PROXIES=0

# Hosts permitidos para validación Origin/Referer (separados por coma, SIN espacios)
# Debe incluir host:puerto si usas puerto no estándar
# Ejemplo producción: ALLOWED_HOSTS=app.example.com,admin.example.com
# Ejemplo desarrollo: ALLOWED_HOSTS=localhost:6081,127.0.0.1:6081
# Si no se configura, se usa request.host (requiere ProxyFix correcto)
ALLOWED_HOSTS=localhost:6081
